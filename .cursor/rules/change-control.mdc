---
description: 最小闭环交付、变更范围控制与代码评审清单
alwaysApply: true
---

# 变更与重构规则

> 作为 AGENTS.md 变更范围控制的补充，用于评估改动范围、评审风险与交付完整性。

**AI 决策前应首先检查本规则。**

---

## 必须做

- 以**最小闭环交付**为原则：只改完成需求所必需的文件
- 动手前先理解现有实现、调用链与测试覆盖
- 行为变更在交付说明中明确描述（见 AGENTS.md 输出模板）
- 破坏性变更（API、schema、字段语义）须显式声明并提供迁移方案
- 抽取重复逻辑仅限当前需求范围内的最小合理粒度
- 提交前自检：lint、相关测试、无明显回归
- 存在假设、未验证项、环境限制时诚实说明

## 禁止做

- 大面积重命名、跨模块无关重构
- 风格化批量格式化（与需求无关的 prettier/eslint 全量 fix）
- 引入新框架替代现有技术栈
- 没有业务收益的大规模"架构优化"
- 偷改外部接口、事件结构、数据库字段语义
- 为"看起来更优雅"移动大量文件
- 虚构验证结果
- 未经说明直接提交 commit 或 push

---

## 改动范围决策

```
收到需求
  │
  ├─ 能否在现有模块内完成？ ──否──→ 先确认架构，再最小扩展
  │         │
  │        是
  │         ↓
  ├─ 是否需要新依赖？ ──是──→ 说明引入理由，优先用现有栈
  │         │
  │        否
  │         ↓
  ├─ 是否触及公共接口/schema？ ──是──→ 补契约测试 + 迁移说明
  │         │
  │        否
  │         ↓
  └─ 执行最小改动 → 补测试 → 自检 → 按模板交付
```

---

## 允许 vs 默认不允许

| 允许 | 默认不允许 |
|------|------------|
| 需求必要的小范围抽取 | 全项目统一命名风格变更 |
| 抽私有方法提升可读性 | 多模块目录结构重组 |
| 与周边代码一致的小整理 | 替换 ORM / 状态库 / 测试框架 |
| 修复改动触及文件的明显 lint 问题 | 单独 PR 做纯格式化 |
| 新增文件遵循现有目录约定 | 自创与现有约定冲突的目录 |

---

## 代码评审清单

### 通用

- [ ] 改动范围与需求匹配，无无关 diff
- [ ] 未删除/修改已合并迁移或破坏 git 历史
- [ ] 无硬编码密钥、环境相关 magic value
- [ ] 交付说明包含：文件、行为、原因、验证、假设、风险

### 接口与契约

- [ ] GraphQL schema 变更向后兼容或有 version 策略
- [ ] 未静默放宽权限校验
- [ ] 事件/消息结构变更Consumers 已评估
- [ ] 公共类型导出变更已搜索调用方

### 分层

- [ ] 业务逻辑在 Service 层，不在 Resolver / Model
- [ ] ORM 层无流程编排
- [ ] 前端组件无后端业务逻辑

### 数据

- [ ] Schema 变更伴随迁移脚本
- [ ] 无未参数化 SQL
- [ ] 事务边界正确，无长事务夹外部调用

### 测试

- [ ] 按风险补了对应层级测试
- [ ] Bug fix 有回归测试
- [ ] 未虚构测试结果

### 可观测性

- [ ] 关键路径有日志 + traceId
- [ ] AI 调用有 Langfuse 关联
- [ ] 无敏感信息泄露

### 安全

- [ ] 输入边界校验
- [ ] SQL 执行经权限与审计
- [ ] 错误信息不暴露内部细节

---

## 重构触发条件

仅在以下情况考虑 refactor，且范围限于触达模块：

1. **重复代码**已在 3+ 处出现且抽取可立即减少本次改动复杂度
2. **现有结构阻塞需求**，不 refactor 无法实现
3. **明确 bug 根因**是结构问题，最小修复需局部整理

其他情况：先交付功能，refactor 另开任务。

---

## 示例：范围判断

```markdown
# ❌ 过度改动
- 重命名整个 services/ 目录为 usecases/
- 将所有 var 改为 const 的全文件格式化
- 引入 Zustand 替代现有 2 处 useState
- 顺带升级 Next.js 14 → 15

# ✅ 最小闭环
- 新增 ReportService.generate()
- 新增 graphql/resolvers/report.ts 薄封装
- 新增 tests/services/report/generate.test.ts
- 更新 schema.graphql 新增 mutation
```

---

## 与 AGENTS.md 的关系

- 本规则是 AGENTS.md「变更范围控制」的**操作层补充**
- 冲突时以 AGENTS.md 为准
- 具体技术细节见同目录其他规则：`frontend.mdc`、`backend-api.mdc`、`database.mdc`、`testing.mdc`、`observability.mdc`
